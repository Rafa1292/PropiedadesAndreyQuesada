@page "/Cantons/Delete/{CantonId}"

<DeleteCantonComponent CantonId="@CantonId" />

@code {
    [Parameter]
    public string CantonId { get; set; }

    protected override async Task OnInitializedAsync()
    {
        CantonId = CantonId;
    }
}